
# Модель Team

## 📋 Основная информация
- **Таблица:** `teams`
- **Назначение:** Управление командами участников
- **Макс. размер:** 5 человек

## 🗃️ Структура таблицы
| Поле | Тип | Nullable | Default | Описание |
|------|-----|----------|---------|----------|
| id | INTEGER | ❌ | auto | Первичный ключ |
| name | VARCHAR(100) | ❌ | - | Название команды |
| captain_id | INTEGER | ❌ | - | FK → users.id |
| mentor_id | INTEGER | ✅ | NULL | FK → users.id |

## Связи
```python
# SQLAlchemy relationships
captain = relationship("User", foreign_keys="[Team.captain_id]")
mentor = relationship("User", foreign_keys="[Team.mentor_id]")
members = relationship("User", back_populates="team")


## Бизнес-правила: Управление командами

## Формирование команд
1. Максимальный размер: 5 человек
2. Обязательные роли: Капитан
3. Опциональные роли: Ментор

## Процесс вступления
1. Условия для вступления:
   - Команда не полная (<5 человек)
   - Пользователь не состоит в другой команде
   - Пользователь имеет статус `looking_for_team`

2. Ограничения:
   - Капитан не может покинуть команду (должен передать капитанство)
   - Пользователь не может быть капитаном и ментором одновременно

## Анкеты участников
2. Активация анкеты:
   - `profile_text` не пустой
   - `team_id` IS NULL
   - `participant_status` = 'looking_for_team'


2. Деактивация автоматически:
- При вступлении в команду
- При очистке текста анкеты


