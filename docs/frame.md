
## 📋 Таблица `users` (Анкеты участников)

### Назначение
Хранит информацию обо всех пользователях бота: участниках, организаторах, менторах, волонтёрах.

### Структура
| Столбец | Тип | Обязательно | Описание |
|---------|-----|-------------|----------|
| `id` | INTEGER | ✅ | Первичный ключ |
| `telegram_id` | BIGINT | ✅ | Уникальный ID пользователя в Telegram |
| `username` | VARCHAR(100) | ❌ | Имя пользователя в Telegram |
| `full_name` | VARCHAR(200) | ✅ | Полное имя пользователя |
| `role` | ENUM | ✅ | Роль: `participant`, `organizer`, `mentor`, `volunteer` |
| `email` | VARCHAR(100) | ❌ | Email пользователя |
| `phone` | VARCHAR(20) | ❌ | Номер телефона |
| `telegram_username` | VARCHAR(100) | ❌ | Username для связи |
| `desired_role` | VARCHAR(100) | ❌ | Желаемая роль в команде |
| `skills` | ARRAY(VARCHAR) | ❌ | Навыки и экспертиза |
| `interests` | ARRAY(VARCHAR) | ❌ | Интересы по темам |
| `experience` | TEXT | ❌ | Опыт работы/участия |
| `bio` | TEXT | ❌ | О себе |
| `participant_status` | ENUM | ❌ | Статус: `looking_for_team`, `in_team`, `not_looking` |
| `team_id` | INTEGER | ❌ | Внешний ключ к таблице `teams` |
| `timezone` | VARCHAR(50) | ✅ | Часовой пояс (по умолчанию: UTC+3) |
| `is_active` | BOOLEAN | ✅ | Активен ли пользователь |
| `registered_at` | TIMESTAMP | ✅ | Дата регистрации |

### Индексы
- `idx_users_telegram_id` - для быстрого поиска по Telegram ID
- `idx_users_team_id` - для поиска участников команды

## 🏆 Таблица `teams` (Команды хакатона)

### Назначение
Хранит информацию о командах участников.

### Структура
| Столбец | Тип | Обязательно | Описание |
|---------|-----|-------------|----------|
| `id` | INTEGER | ✅ | Первичный ключ |
| `name` | VARCHAR(100) | ✅ | Название команды |
| `description` | TEXT | ❌ | Описание команды |
| `project_theme` | VARCHAR(200) | ❌ | Тематика проекта |
| `max_members` | INTEGER | ✅ | Максимальное количество участников (по умолчанию: 5) |
| `current_members_count` | INTEGER | ✅ | Текущее количество участников |
| `needed_roles` | ARRAY(VARCHAR) | ❌ | Необходимые/нехватающие роли |
| `contact_info` | VARCHAR(200) | ❌ | Контакт для связи |
| `is_active` | BOOLEAN | ✅ | Активна ли команда |
| `is_full` | BOOLEAN | ✅ | Заполнена ли команда |
| `captain_id` | INTEGER | ✅ | Внешний ключ к `users.id` (капитан) |
| `created_at` | TIMESTAMP | ✅ | Дата создания |

### Индексы
- `idx_teams_captain_id` - для поиска команд по капитану
- `idx_teams_is_active` - для фильтрации активных команд

## 📝 Таблица `team_applications` (Заявки в команды)

### Назначение
Управляет процессом подачи заявок участниками в команды.

### Структура
| Столбец | Тип | Обязательно | Описание |
|---------|-----|-------------|----------|
| `id` | INTEGER | ✅ | Первичный ключ |
| `user_id` | INTEGER | ✅ | Внешний ключ к `users.id` |
| `team_id` | INTEGER | ✅ | Внешний ключ к `teams.id` |
| `message` | TEXT | ❌ | Сообщение от участника |
| `status` | ENUM | ✅ | Статус: `pending`, `accepted`, `rejected`, `cancelled` |
| `applied_at` | TIMESTAMP | ✅ | Дата подачи заявки |
| `reviewed_at` | TIMESTAMP | ❌ | Дата рассмотрения |
| `response_message` | TEXT | ❌ | Ответ команды |

### Индексы
- `idx_applications_user_id` - для поиска заявок пользователя
- `idx_applications_team_id` - для поиска заявок команды
- `idx_applications_status` - для фильтрации по статусу

## 🔗 Связи между таблицами

### 1. users → teams (Один ко многим)
- **users.team_id** → **teams.id**
- Один пользователь может быть только в одной команде
- В одной команде может быть много пользователей

### 2. users → team_applications (Один ко многим)
- **team_applications.user_id** → **users.id**
- Один пользователь может подать много заявок
- Каждая заявка от одного пользователя

### 3. teams → team_applications (Один ко многим)
- **team_applications.team_id** → **teams.id**
- В одну команду может прийти много заявок
- Каждая заявка в одну команду

### 4. teams → users (Один к одному)
- **teams.captain_id** → **users.id**
- У каждой команды один капитан
- Капитан - это пользователь

## 📈 Бизнес-логика

### Процесс вступления в команду:
1. Участник (`User`) с `participant_status = 'looking_for_team'` подаёт заявку (`TeamApplication`)
2. Заявка создаётся со статусом `pending`
3. Капитан команды рассматривает заявку
4. При принятии: статус → `accepted`, участник добавляется в команду
5. При отклонении: статус → `rejected`

### Обновление количества участников:
При добавлении/удалении участника из команды:
1. Обновляется `users.team_id`
2. Обновляется `teams.current_members_count`
3. Проверяется `teams.is_full`

## 🔒 Ограничения целостности

1. **Внешние ключи** - гарантируют, что заявки ссылаются на существующих пользователей и команды
2. **Уникальность** - `telegram_id` уникален для каждого пользователя
3. **Каскадное удаление** - при удалении пользователя/команды удаляются связанные заявки
