# 🔗 Связи между таблицами

## 1. User ↔ Team
### User → Team (Многие к одному)
- **users.team_id** → **teams.id**
- Один пользователь может быть только в одной команде
- В одной команде может быть много пользователей (до 5)

### Team → User (Один к одному)
- **teams.captain_id** → **users.id**
- У каждой команды ровно один капитан

## 2. User ↔ Event
### Event → User (Многие к одному)
- **events.created_by** → **users.id**
- Каждое событие создано одним пользователем (организатором)
- Один пользователь может создать много событий

## 3. User ↔ Poll
### Poll → User (Многие к одному)
- **polls.creator_id** → **users.id**
- Каждый опрос создан одним пользователем (организатором)
- Один пользователь может создать много опросов

### PollVote → User (Многие к одному)
- **poll_votes.user_id** → **users.id** (ON DELETE CASCADE)
- Каждый голос принадлежит одному пользователю
- Один пользователь может проголосовать во многих опросах

### PollMessage → User (Многие к одному)
- **pollmsgs.user_id** → **users.id**
- Каждое сообщение с опросом отправлено одному пользователю
- Один пользователь может получить много сообщений с опросами

## 4. User ↔ Notification
### EventNotification → User (Многие к одному)
- **event_notifications.user_id** → **users.id**
- Каждое уведомление отправлено одному пользователю
- Один пользователь может получить много уведомлений

### NotificationSettings ↔ User (Один к одному)
- **notification_settings.user_id** → **users.id**
- Каждый пользователь имеет одни настройки уведомлений
- Настройки принадлежат только одному пользователю

## 5. Event ↔ Related Tables
### EventNotification → Event (Многие к одному)
- **event_notifications.event_id** → **events.id**
- Каждое уведомление относится к одному событию
- Одно событие может иметь много уведомлений

## 6. Poll ↔ Related Tables
### PollVote → Poll (Многие к одному)
- **poll_votes.poll_id** → **polls.id**
- Каждый голос относится к одному опросу
- Один опрос может иметь много голосов

### PollMessage → Poll (Многие к одному)
- **pollmsgs.poll_id** → **polls.id**
- Каждое сообщение относится к одному опросу
- Один опрос может быть отправлен в виде многих сообщений

## 7. User → Created Records
### User → Event (Один ко многим)
- Пользователь может создать много событий

### User → EventNotification (Один ко многим)
- Пользователь может получить много уведомлений
- Уведомления пользователя доступны через `user.event_notifications`

