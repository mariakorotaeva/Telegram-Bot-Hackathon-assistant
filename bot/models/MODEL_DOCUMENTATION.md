# Документация по модели AI-ассистента

## Модель: Qwen2.5 7B Instruct

### Основные характеристики:
- **Базовая модель**: `qwen2.5:7b-instruct` от Alibaba Cloud
- **Архитектура**: Transformer-based decoder-only
- **Контекстное окно**: 32K токенов (ограничено до 2048 в конфигурации)
- **Язык**: Поддержка русского и английского
- **Размер модели**: ~4.7 GB (quantized)
- **Лицензия**: Apache 2.0

### Технические параметры:

| Параметр | Значение | Описание |
|----------|----------|----------|
| **temperature** | 0.3 | Низкое для детерминированных ответов |
| **top_p** | 0.9 | Ядерная выборка для разнообразия |
| **top_k** | 40 | Ограничение словаря |
| **num_predict** | 512 | Максимальное количество токенов в ответе |
| **repeat_penalty** | 1.05 | Штраф за повторения |
| **num_ctx** | 2048 | Размер контекста |
| **seed** | 42 | Seed для воспроизводимости |

## Подключение модели

### 1. Установка Ollama

```bash
# Linux/macOS
curl -fsSL https://ollama.com/install.sh | sh

# Windows: скачать с https://ollama.com/download/windows
```

### 2. Создание кастомной модели

**Файл конфигурации:** `models/qwen2.5:7b-model.Modelfile`

```bash
# 1. Загрузите базовую модель
ollama pull qwen2.5:7b-instruct

# 2. Создайте кастомную модель
ollama create hackathon-assistant -f ./models/qwen2.5:7b-model.Modelfile

# 3. Проверьте создание
ollama list
```

### 3. Запуск модели

```bash
ollama serve &
```

## Интеграция с ботом

### Класс OllamaHandler (`ollama_handler.py`)

```python
class OllamaHandler:
    def __init__(self):
        self.model_name = os.getenv('OLLAMA_MODEL', 'hackathon-assistant:latest')
        self.host = os.getenv('OLLAMA_HOST', 'http://localhost:11434')
```

### Переменные окружения:

```env
OLLAMA_MODEL=hackathon-assistant:latest
OLLAMA_HOST=http://localhost:11434
RESPONSE_TIMEOUT=350
```

## 🎛️ Настройка генерации

### Параметры в коде:

```python
options = {
    "temperature": 0.7,
    "num_predict": 200,
    "top_k": 40,
    "top_p": 0.9,
    "repeat_penalty": 1.1,
    "num_thread": 4,
}
```

### Рекомендации по параметрам:

| Сценарий | temperature | num_predict |
|----------|-------------|-------------|
| **FAQ ответы** | 0.1-0.3 | 100-150 |
| **Поиск команды** | 0.4-0.6 | 200-300 |
| **Поддержка** | 0.3-0.5 | 150-250 |
| **Объявления** | 0.7-0.9 | 300-400 |

## 🔌 API эндпоинты

```
POST /api/generate     # Генерация текста
GET  /api/tags         # Список моделей
```

### Пример запроса:

```bash
curl http://localhost:11434/api/generate -d '{
  "model": "hackathon-assistant:latest",
  "prompt": "Какое расписание на сегодня?",
  "stream": false,
  "options": {
    "temperature": 0.3,
    "num_predict": 200
  }
}'
```

## 📝 Примечания

### 1. Поддержка русского языка
- Все промпты на русском
- Шаблон `<|im_start|>system` для структурирования
- Явное требование в системном промпте отвечать только на русском

### 2. Контекстное окно
Ограничено до 2048 токенов для экономии RAM и быстрой обработки.

### 3. Обновление модели

```bash
# Обновление базовой модели
ollama pull qwen2.5:7b-instruct

# Пересоздание кастомной
ollama rm hackathon-assistant
ollama create hackathon-assistant -f ./models/qwen2.5:7b-model.Modelfile
```

---